spring:
  application:
    name: TeluskoApp1
  cloud:
    compatibility-verifier.enabled: false
  boot:
    admin:
      client:
        url: http://localhost:1414/
      
      
    
server:
  port: 8484

management:
  endpoints:
    web:
      exposure:
        include: '*'
        

